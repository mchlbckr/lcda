# lcda

Latent class discriminant analysis for categorical data.

## Installation

CRAN:

```r
install.packages("lcda")
```

Development version:

```r
remotes::install_github("mchlbckr/lcda")
```

## Usage

```r
library(lcda)

# See ?lcda, ?cclcda, and ?cclcda2 for examples
```
